# ESG RAG Pipeline — Python dependencies
# Extracted from the ddl_venv environment on the HPC cluster.
# GPU execution requires CUDA 12.x and an NVIDIA GPU with ≥80GB VRAM
# (tested on H100 80GB SXM5) for the large quantized models.
#
# Install:
#   pip install -r requirements.txt

# ── PDF processing ─────────────────────────────────────────────────────────────
pdfplumber==0.11.9
pdfminer.six==20251230        # pdfplumber dependency

# ── Embeddings & vector search ─────────────────────────────────────────────────
sentence-transformers==5.1.1
faiss-cpu==1.14.2
numpy==1.26.4

# ── Hybrid retrieval ───────────────────────────────────────────────────────────
rank-bm25==0.2.2              # BM25 keyword retrieval alongside FAISS

# ── LLM inference ─────────────────────────────────────────────────────────────
vllm==0.11.2
torch==2.9.0
transformers==4.56.1
accelerate==1.10.1
bitsandbytes==0.49.2          # 4-bit quantization for 70B+ models
tokenizers==0.22.0
safetensors==0.6.2

# ── Data handling ─────────────────────────────────────────────────────────────
pandas==2.3.2
tqdm==4.67.1
